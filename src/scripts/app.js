define([
	'underscore'
	, 'backbone'
	, 'game/game'
	, 'localforage'
	// DEPS
	, 'jquery.requestAnimationFrame'
], function (_, Backbone, Game, localforage) {
	'use strict';

	var App = Game.initialize();
});
